name: Api-client Package

on:
  push:
    branches: ['staging/api-client']

jobs:
  version:
    runs-on: ubuntu-latest
    steps:
      - name: Branch name
        run: echo "branch=$(echo ${GITHUB_REF#refs/heads/})" >>$GITHUB_OUTPUT
        id: current_branch
      - uses: actions/checkout@v3
      - run: ls -al
      - run: cat package.json
